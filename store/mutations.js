export default {
  // PRICING_BLOCKS(state, blocks) {
  //   state.pricingBlock = blocks
  // },
  // PRICING_BLOCKS(state, { lang, block }) {
  //   state.pricingBlock = {
  //     ...state.pricingBlock,
  //     [lang]: block
  //   }
  // },
  SET_LANGUAGES(state, languages) {
    state.languages = languages
  }
}
